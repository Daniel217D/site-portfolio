name: Deploy

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest

    steps:
      - name: Deployment is not configured yet
        run: echo "Production deployment target is not defined yet."

